import React from 'react';

const Blogs = () => {
    return (
        <div className='container'>
            <h1 className='text-center my-5'>QnA Section</h1>
            <h2>1. What is the difference between javascript and node.js?</h2>
            <p>-- The main difference between javascript and node.js is javascript is a simple programming language, but node.js is a javascript runtime environment. Also, javascript uses on the client-side. Node.js is used on the server-side. So, javascript uses for frontend development, and node.js is uses for server-side development.</p>
            <h2>2. What are the differences between SQL and noSQL databases?</h2>
            <p>-- The main difference between javascript and node.js is javascript is a simple programming language, but node.js is a javascript runtime environment. Also, javascript uses on the client-side. Node.js is used on the server-side. So, javascript uses for frontend development, and node.js is uses for server-side development.</p>
        </div>
    );
};

export default Blogs;